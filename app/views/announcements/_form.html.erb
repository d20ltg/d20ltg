<br />
<%= semantic_form_for(@announcement, :html => { :multipart => true }) do |f| %>
  <%= f.inputs do %>
    <%= f.input :title %>
  <% end %>
  <%= f.label :image %>
  <%= f.file_field :image %>
  <br />
  <%= f.action :submit, as: :button, :disable_with => "Saving..." %>
<% end %>
<br />
